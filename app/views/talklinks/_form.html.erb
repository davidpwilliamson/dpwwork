<%= form_for(@talklink) do |f| %>
  <% if @talklink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@talklink.errors.count, "error") %> prohibited this talklink from being saved:</h2>

      <ul>
      <% @talklink.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :talklinktype_id %><br />
    <%= f.number_field :talklinktype_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
