<%= form_for(@publinktype) do |f| %>
  <% if @publinktype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@publinktype.errors.count, "error") %> prohibited this publinktype from being saved:</h2>

      <ul>
      <% @publinktype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
