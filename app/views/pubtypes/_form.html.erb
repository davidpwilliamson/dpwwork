<%= form_for(@pubtype) do |f| %>
  <% if @pubtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pubtype.errors.count, "error") %> prohibited this pubtype from being saved:</h2>

      <ul>
      <% @pubtype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
